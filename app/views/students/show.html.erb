<h1>Student name: <%= @student.name %></h1>

<section class="courses">
  <% @student.courses.each do |course| %>
    <p class="grades"><%= course.name %>: <%= @student.grade_for_course(course.id).join %></p>
  <% end %>
</section>
